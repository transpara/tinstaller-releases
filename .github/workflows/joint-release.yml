name: Generate Platform Release Notes
on:
  push:
    tags:
      - '*.*.*'
  workflow_dispatch:

jobs:
  build-release-notes:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Generate Notes
        env:
          GH_TOKEN: ${{ secrets.READ_ALL_TOKEN }}
          ORG: "transpara"
          INSTALLER_REPO: "tinstaller-releases"
          OUTPUT_FILE: "PLATFORM_RELEASE_NOTES.md"
        run: |
          # Resolve the target tag from the trigger context
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            bash scripts/generate-release-notes.sh "${{ github.ref_name }}"
          else
            bash scripts/generate-release-notes.sh
          fi

          echo "::group::Generated Release Notes"
          cat PLATFORM_RELEASE_NOTES.md
          echo "::endgroup::"

          # Rename with version suffix for asset download clarity
          cp PLATFORM_RELEASE_NOTES.md "PLATFORM_RELEASE_NOTES-${{ env.target_tag }}.md"

      - name: Update Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.target_tag }}
          body_path: PLATFORM_RELEASE_NOTES.md
          append_body: true
          files: PLATFORM_RELEASE_NOTES-${{ env.target_tag }}.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
